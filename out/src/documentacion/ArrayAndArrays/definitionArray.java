package documentacion.ArrayAndArrays;

public class definitionArray {

    /*
---¿QUÉ ES UN ARRAY?---

Un array en Java es una estructura de datos que se utiliza para almacenar una colección de elementos del mismo tipo.
Puedes pensar en un array como una serie ordenada de elementos, donde cada elemento tiene un índice único que indica
su posición en el array. Los arrays en Java tienen un tamaño fijo que se especifica al momento de su creación y no
puede cambiarse después.

La estructura básica de un array en Java se define de la siguiente manera:
------------------------------------------------------------------------------------------------------------------------
tipo[] nombreArray = new tipo[tamaño];
Donde:

tipo es el tipo de datos de los elementos que se almacenarán en el array.
nombreArray es el nombre que le das al array.
tamaño es el número de elementos que puede contener el array.

Por ejemplo, puedes crear un array de enteros con 5 elementos de la siguiente manera:

int[] numeros = new int[5];
Esto crea un array llamado numeros que puede contener 5 números enteros. Los elementos del array se numeran desde 0
hasta el tamaño del array menos 1. Puedes acceder a los elementos individuales del array utilizando su índice. Por
ejemplo, para acceder al primer elemento del array numeros, utilizarías numeros[0].
------------------------------------------------------------------------------------------------------------------------
Los arrays en Java pueden contener cualquier tipo de datos, incluidos tipos primitivos como int, double, char, etc.,
y también objetos. Además, puedes inicializar un array con valores iniciales en el momento de su creación,
proporcionando los valores entre llaves {}. Por ejemplo:

int[] numeros = {1, 2, 3, 4, 5};
Esto crea un array de enteros llamado numeros con 5 elementos inicializados con los valores 1, 2, 3, 4 y 5.
     */
}
